# entorno
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# salidas del motor
out/
*.3mf
*.stl
*.glb

# credenciales locales — hashes de usuarios para el login.
# Nunca se commitea: es por maquina, no por repo.
credenciales.json

# secreto de firma de la sesion — se genera solo en el primer arranque
sesion.key

# directorios de trabajo de la web: uno por UUID, efimeros y con TTL
trabajo/

# herramientas
.pytest_cache/
.mypy_cache/
.ruff_cache/

# basura de windows / git bash
bash.exe.stackdump
Thumbs.db
desktop.ini
